{
  "name": "gitimpact",
  "private": true,
  "version": "0.1.0",
  "description": "Know what breaks before you merge — blast-radius analysis for code changes",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "build:packages": "pnpm --filter @gitimpact/shared build && pnpm --filter @gitimpact/git build && pnpm --filter @gitimpact/parser build && pnpm --filter @gitimpact/framework-detector build && pnpm --filter @gitimpact/graph build && pnpm --filter @gitimpact/impact-engine build && pnpm --filter @gitimpact/db build && pnpm --filter @gitimpact/ops build && pnpm --filter @gitimpact/queue build && pnpm --filter @gitimpact/analysis build && pnpm --filter @gitimpact/cli build",
    "db:up": "docker compose up -d postgres redis",
    "db:down": "docker compose down",
    "db:push": "pnpm --filter @gitimpact/db db:push",
    "worker": "pnpm --filter @gitimpact/worker start",
    "worker:dev": "pnpm --filter @gitimpact/worker dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:accuracy": "pnpm --filter @gitimpact/accuracy-tests test",
    "test:real-world": "pnpm --filter @gitimpact/real-world-tests test",
    "test:benchmarks": "pnpm test:accuracy && pnpm test:real-world",
    "dogfood": "pnpm --filter @gitimpact/dogfood dogfood",
    "dogfood:miss": "pnpm --filter @gitimpact/dogfood miss",
    "dogfood:misses": "pnpm --filter @gitimpact/dogfood misses",
    "relationships": "pnpm --filter @gitimpact/relationships relationships",
    "relationships:sample": "pnpm --filter @gitimpact/relationships sample",
    "relationships:review-sample": "pnpm --filter @gitimpact/relationships review-sample",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
