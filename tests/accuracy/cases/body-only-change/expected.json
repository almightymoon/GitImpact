{
  "description": "Internal logic diffs map to enclosing method; semantic engine emits BODY_CHANGED",
  "diffCases": [
    {
      "file": "src/auth.service.ts",
      "patch": "@@ -1,8 +1,8 @@\n export class AuthService {\n   generateToken(payload: string) {\n-    const secret = \"old-secret\";\n-    return sign(payload, oldSecret);\n+    const secret = \"new-secret\";\n+    return sign(payload, secret);\n   }\n }\n",
      "mustEnclose": [
        {
          "kind": "METHOD",
          "name": "generateToken",
          "className": "AuthService"
        }
      ],
      "mustChangedSymbols": [
        {
          "kind": "METHOD",
          "name": "generateToken",
          "className": "AuthService"
        }
      ],
      "mustSemanticEvents": ["BODY_CHANGED"],
      "mustNotSemanticEvents": ["METHOD_REMOVED", "METHOD_ADDED"]
    }
  ]
}
